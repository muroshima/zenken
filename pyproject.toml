[project]
name = "zenken"
version = "0.1.0"
description = "全件を見る経費監査エージェント"
requires-python = ">=3.11"
dependencies = [
    "openai>=1.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "ruff>=0.6",
]

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
testpaths = ["tests"]
